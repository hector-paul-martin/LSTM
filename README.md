# LSTM
This is an LSTM that I made to better understand vector calculus and gradient flow.  It has been written from scratch in NumPy with no high level libraries. This repository contains: The core LSTM, an FFN that I use as an output layer for the LSTM, the code to link the two together and solve a parity problem and a numerical gradient test.
